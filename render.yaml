# Render Blueprint - Backend tek tıkla deploy için (opsiyonel)
services:
  - type: web
    name: sfr-cloud-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: JWT_SECRET
        generateValue: true
