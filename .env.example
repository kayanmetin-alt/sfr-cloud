# Canlı sunucu (Railway veya kendi VM) için örnek. Bu dosyayı .env olarak kopyalayıp değerleri doldurun.
# cp .env.example .env && nano .env

NODE_ENV=production
PORT=3001
SERVE_WEB_STATIC=web/dist

# Üretimde mutlaka güçlü bir değer kullanın (örn: openssl rand -base64 32)
JWT_SECRET=buraya-guclu-rastgele-metin

# İsteğe bağlı: Firestore kullanacaksanız, service account JSON'unu tek satır yapıştırın.
# Yoksa SQLite kullanılır (sfr.db).
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}
